import { LoadingScreen } from "@/components/ui/LoadingScreen";

export default function StaffLoading() {
  return <LoadingScreen variant="dark" message="Personel paneli yükleniyor..." description="Size atanan işler hazırlanıyor." />;
}
